This directory will contain any files that will be imported into markup. 

These files are parsed as-is and should be in ftHTML syntax. 

Templates are intended to consist of frequently used markup that has dynamic information that can be changed with binding properties.

